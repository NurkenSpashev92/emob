# Junior Golang Developer - Effective Mobile

## 📦 О проекте

Проект представляет собой Go-приложение с PostgreSQL, Docker и поддержкой миграций через [golang-migrate](https://github.com/golang-migrate/migrate).  
Сборка и запуск проекта максимально автоматизированы с помощью Makefile.  

---

## ⚡ DEPLOY LOCAL

1. Скопируйте `.env.example` в `.env` и настройте переменные окружения:

```bash
cp devops/.env.example devops/.env

# Установка зависимостей и поднятие проекта
make install

# Или шаг за шагом
make build          # 🔨 Сборка контейнеров
make up             # 🚀 Запуск контейнеров
make migrations_up  # ⬆️ Применение миграций

| Команда                 | Описание                             |
| ----------------------- | ------------------------------------ |
| `make install`          | 🚀 Полная установка и деплой проекта |
| `make up`               | 🚀 Поднять контейнеры в фоне         |
| `make down`             | 🛑 Остановить контейнеры             |
| `make build`            | 🔨 Собрать контейнеры                |
| `make restart`          | 🔁 Перезапустить контейнеры          |
| `make logs`             | 📜 Просмотр логов                    |
| `make ps`               | 📦 Показать запущенные контейнеры    |
| `make app`              | 🐹 Войти в контейнер приложения      |
| `make postgres`         | 🐘 Войти в PostgreSQL                |
| `make create_migration` | 📝 Создать новую миграцию            |
| `make migrations_up`    | ⬆️ Применить миграции                |
| `make clean`            | 🧹 Удалить контейнеры и volume       |
| `make prune`            | 💣 Очистка Docker системы и volume   |


